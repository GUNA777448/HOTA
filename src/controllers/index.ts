// Barrel export for controllers
// Controllers orchestrate business logic between services, models, and views.
// In React, controllers are typically custom hooks that coordinate multiple concerns.
// Example: export { useAuthController } from './auth.controller';
